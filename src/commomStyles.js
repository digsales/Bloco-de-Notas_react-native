export default {
  fontFamily: "Lato",
  colors: {
    secondary: "white",
    mainText: "#222",
    subText: "#555",
  },
  Capitalize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
  },
};
